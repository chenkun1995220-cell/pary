# 人工复核队列活跃判定统一实施计划

**目标：** 消除分类明确且不在候选池的估值事实重复进入人工队列，同时保留候选项、
未分类项和候选名单未知时的保守复核。

## 任务 1：失败测试

- 修改 `tests/test_automation_self_analysis.py`。
- 构造候选池内负 PE、非候选已分类负 PE、非候选未分类三种样本。
- 断言队列仅保留候选池内和未分类样本。
- 构造候选名单未知场景，断言样本仍保留。
- 运行目标测试并确认因当前行为而失败。

## 任务 2：最小修复

- 修改 `automation_self_analysis.py`。
- 解析市场摘要中的候选 ticker，并区分空候选集合与未知候选名单。
- 将候选信息传入健康快照。
- 队列生成时仅排除分类明确的非候选估值项。
- 不修改风险提示、结论缺口和人工决定过滤逻辑。

## 任务 3：验证与交付

- 运行目标测试和 `tests.test_automation_self_analysis`。
- 运行全量测试。
- 检查改动范围，提交并推送当前分支，随后快进合并到 `main`。
