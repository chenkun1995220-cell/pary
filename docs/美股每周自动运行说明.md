# 美股每周自动运行说明

## 自动流程

每次运行会按顺序完成以下工作：

1. 刷新标普 500 成分股名单，失败时使用最后一次有效缓存。
2. 从 SEC 更新全部成分公司的基础信息。
3. 自动补齐价格、流通股数、净负债和行情日期。
4. 更新 SEC 财务指标并执行低估评分。
5. 输出候选公司列表、评分理由和候选公司深研包。

股票池按成分表实际证券行数导入，不强制截断为 500 行。类别股代码会转换为 SEC 和 Yahoo 可识别的连字符格式。Company Facts 缓存在 `data/cache/sec_companyfacts`，行情、财务导入和指标计算共享同一份数据。

总控脚本：

```powershell
powershell.exe -ExecutionPolicy Bypass -File F:\chatgptssd\project2\scripts\run_us_universe_weekly.ps1 -SecUserAgent "Your Name your-email@example.com"
```

只检查执行计划，不联网、不写入文件：

```powershell
powershell.exe -ExecutionPolicy Bypass -File F:\chatgptssd\project2\scripts\run_us_universe_weekly.ps1 -SecUserAgent "Your Name your-email@example.com" -DryRun
```

## 自动化任务

当前拆分为三个独立 Codex 自动化任务，均按北京时间每周日运行：

| 时间 | 任务 | 当前能力 |
|---|---|---|
| 14:05 | 美股低估公司每周筛选 | 标普500行情、SEC财务、评分、候选报告和深研包 |
| 14:25 | A股沪深300每周筛选 | 名单、行情、财务质量、增长、估值评分与候选报告 |
| 14:45 | 港股大中盘每周筛选 | 名单、行情、财务质量、增长、估值评分与候选报告 |

旧 Windows 计划任务已禁用。三个总控脚本使用不同防重入锁、缓存和输出目录。

14:45 的港股任务是三市场周筛收尾任务；港股周筛成功后会继续运行 `run_self_analysis.ps1`、`show_automation_check.ps1`、`show_weekly_action_items.ps1`、`run_weekly_ops_check.ps1`、`show_weekly_ops_history.ps1`、`show_weekly_conclusion.ps1`、`run_weekly_delivery_check.ps1` 和 `show_weekly_delivery_history.ps1`，最终回复优先展示周度运维总检查、历史趋势摘要、统一每周结论报告、最终交付验收、最终交付历史摘要和每周人工处理清单，再补充一屏验收结论和港股明细。

A股和港股当前使用 `regional_fundamental_v2` 模型，综合同行业PE/PB折价、ROE、ROIC、毛利率、资产负债率、流动比率、经营现金流和收入利润增长。候选公司要求PE、PB为正、ROE不低于5%、财务数据可用、行业样本不少于5家且总分不低于75分。

V2模型仍未包含审计意见、治理事件、商誉、股权质押、AH股溢价和完整自由现金流，因此候选结果是基本面量化初筛，不替代原始财报核验。

## A股与港股股票池

A股使用中证指数公司发布的沪深300成分文件，港股使用恒生指数公司 `sizeindexes` 数据中的 HSLI 与 HSMI 完整成分列表。在线校验失败时使用最后一次有效缓存，没有有效缓存时停止运行。

```text
data/samples/cn_universe_companies.csv
data/cache/csi300
outputs/cn_universe/latest_run_summary.md
outputs/cn_universe/candidate_pool.csv
outputs/cn_universe/weekly_report.md
outputs/cn_universe/financial_snapshot.csv
outputs/cn_universe/quote_gaps.csv
outputs/cn_universe/quote_gaps.md
outputs/cn_universe/quote_retry.md
outputs/cn_universe/valuation_review_items.csv

data/samples/hk_universe_companies.csv
data/cache/hk_large_mid
outputs/hk_universe/latest_run_summary.md
outputs/hk_universe/candidate_pool.csv
outputs/hk_universe/weekly_report.md
outputs/hk_universe/financial_snapshot.csv
outputs/hk_universe/quote_gaps.csv
outputs/hk_universe/quote_gaps.md
outputs/hk_universe/quote_retry.md
outputs/hk_universe/valuation_review_items.csv
```

## 输出位置

```text
outputs/us_universe/candidate_pool.csv
outputs/us_universe/screening_results.csv
outputs/us_universe/reports
outputs/us_universe/research
outputs/us_universe/latest_run_summary.md
outputs/us_universe/latest_investment_summary.md
outputs/us_universe/run_*.log
outputs/automation/latest_self_analysis.md
outputs/automation/latest_self_analysis_manifest.json
outputs/automation/latest_automation_check.json
outputs/automation/latest_weekly_action_items.json
outputs/automation/latest_weekly_action_items.md
outputs/automation/latest_weekly_ops_check.json
outputs/automation/weekly_ops_check_history.jsonl
outputs/automation/latest_weekly_ops_history_summary.json
outputs/automation/latest_weekly_ops_history_report.md
outputs/automation/latest_weekly_conclusion.md
outputs/automation/latest_weekly_conclusion.json
outputs/automation/latest_weekly_delivery_check.json
outputs/automation/weekly_delivery_check_history.jsonl
outputs/automation/latest_weekly_delivery_history_summary.json
outputs/automation/latest_weekly_delivery_history_report.md
outputs/automation/latest_manual_review_queue.csv
outputs/automation/manual_review_queue_history.csv
outputs/automation/manual_review_repeats.csv
outputs/automation/manual_review_decisions.csv
outputs/automation/manual_review_decisions_template.csv
outputs/automation/latest_manual_review_decision_merge.json
outputs/automation/latest_manual_review_decision_merge.md
data/cache/sp500/sp500_constituents.csv
data/cache/sp500/sp500_refresh_metadata.json
data/cache/sec_companyfacts
```

候选列表是量化初筛结果，不构成投资建议。深研前仍需核验原始 SEC 文件、一次性项目、行业周期和估值假设。

## 每周自我分析摘要

三市场周筛和严格时点回测完成后，可运行统一自我分析入口：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File F:\chatgptssd\project2\scripts\run_self_analysis.ps1
```

只读取本周验收 JSON 并输出一屏中文结论：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File F:\chatgptssd\project2\scripts\show_automation_check.ps1
```

只读取自我分析 manifest，把 `automation_priority_actions` 转成每周人工处理清单：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File F:\chatgptssd\project2\scripts\show_weekly_action_items.ps1
```

审计三条 Codex 自动任务配置是否仍为启用、时间正确、工作区正确且港股任务包含统一验收入口：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File F:\chatgptssd\project2\scripts\audit_codex_automations.ps1
```

汇总验收文件、关键输出路径和三条 Codex 自动任务配置，输出周度运维总检查：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File F:\chatgptssd\project2\scripts\run_weekly_ops_check.ps1
```

读取周度运维总检查历史，输出最近窗口的趋势摘要：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File F:\chatgptssd\project2\scripts\show_weekly_ops_history.ps1
```

读取最终交付验收历史，输出最近窗口的交付稳定性摘要：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File F:\chatgptssd\project2\scripts\show_weekly_delivery_history.ps1
```

生成统一每周结论报告，汇总三市场候选、目标价、建议买入价、风险理由和自动化验收状态：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File F:\chatgptssd\project2\scripts\show_weekly_conclusion.ps1
```

文档路径写法：`scripts\show_weekly_conclusion.ps1`。该入口只读取三市场已经生成的 `latest_run_summary.md`、`candidate_pool.csv`、`valuation_targets.csv`、`valuation_report.md`、`latest_investment_summary.md`、自动化验收 JSON、`latest_weekly_delivery_history_summary.json` 以及可选的 `outputs/automation/manual_review_decisions.csv`；它会在周结论中展示最终交付历史状态和 `overall_health` 工具健康分数，但不重新抓取行情、不重新评分、不修改正式模型参数。默认输出 `outputs/automation/latest_weekly_conclusion.md`、`outputs/automation/latest_weekly_conclusion.json` 和 `outputs/automation/manual_review_decisions_template.csv`，用于每周最终阅读入口、后续仪表盘或归档流程。`overall_health` 只反映自动化运行、交付历史和人工复核待办是否需要处理，不代表候选公司的投资价值。内容仅供研究筛选和人工复核，不构成投资建议。

生成周结论后，运行最终交付验收，确认 Markdown 周报、JSON 周报和人工复核模板已生成且日期不超过默认 8 天：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File F:\chatgptssd\project2\scripts\run_weekly_delivery_check.ps1
```

文档路径写法：`scripts\run_weekly_delivery_check.ps1`。该入口只读取 `outputs/automation/latest_weekly_conclusion.json` 中声明的最终输出路径、`overall_health` 工具健康分数、每周人工处理清单以及可选的人工复核合并摘要；它不重新抓取行情、不重新评分、不修改正式模型参数。默认输出 `outputs/automation/latest_weekly_delivery_check.json`，字段包含 `delivery_check_schema`、`delivery_check_version`、总状态、周结论新鲜度、周结论健康状态、候选总数、人工复核队列数量、待处理复核数量、`action_items_status`、`action_items_freshness_status`、`action_items_count`、缺失输出和需要处理的原因；当周结论健康状态为 `needs_review` 时只记录提示，当状态为 `needs_fix` 时会追加 `conclusion_health_needs_fix` 并把最终交付验收标为需要处理；当 `latest_weekly_action_items.json` 或 `latest_weekly_action_items.md` 缺失、结构异常或日期超过默认 8 天时，也会把最终交付验收标为需要处理；同时追加 `outputs/automation/weekly_delivery_check_history.jsonl`，用于保留每次最终交付验收的历史轨迹。

`weekly_delivery_history_report.py` 和 `scripts/show_weekly_delivery_history.ps1` 只读取 `weekly_delivery_check_history.jsonl`，输出 `outputs/automation/latest_weekly_delivery_history_summary.json` 和 `outputs/automation/latest_weekly_delivery_history_report.md`。该摘要同样按 `as_of_date` 取最后一条记录，统计最近窗口的 ready 次数、needs_attention 次数、stale 次数、重复出现的最终交付问题、`recurring_health_reasons` 重复健康原因，以及 `latest_action_items_status`、`latest_action_items_freshness_status`、`latest_action_items_count`、`action_items_ready_count`、`action_items_problem_count` 和 `recurring_action_items_issues`，用于判断最终周报交付是否稳定，并区分反复出现的是文件交付问题、人工复核/数据健康提示，还是每周人工处理清单本身缺失、过期或结构异常。它不抓取行情、不重新评分、不修改模型参数。

该脚本读取三市场 `latest_run_summary.md`、三市场 `model_audit.md`、三市场 `data_health_history.csv`、三市场 `quote_gaps.csv`、三市场 `valuation_review_items.csv` 和美股 `latest_backtest_summary.md`，输出 `outputs/automation/latest_self_analysis.md`、`outputs/automation/latest_self_analysis_manifest.json`、`outputs/automation/latest_automation_check.json`、`outputs/automation/latest_manual_review_queue.csv`、`outputs/automation/manual_review_queue_history.csv` 和 `outputs/automation/manual_review_repeats.csv`。自我分析摘要只汇总运行状态、数据健康、缺口和下周优化建议；行情覆盖、财务覆盖、刷新状态或数据质量出现异常时，只进入人工复核，不自动修改正式模型参数。manifest 记录 `manifest_schema`、`manifest_version`、本次日期、输出路径、`automation_status`、`automation_recommended_action`、`automation_priority_actions`、三市场 `markets` 运行快照、`model_audit_status`、`model_audit_recommended_action`、`backtest_status`、`backtest_recommended_action`、三市场 `health` 数据健康快照、`data_health_status`、`data_health_recommended_action`、`candidate_review_status`、`candidate_review_recommended_action`、人工复核队列数量、历史重复项数量、`review_status` 和 `recommended_next_action`，供后续自动任务或仪表盘读取。`latest_automation_check.json` 是本周运行验收入口，汇总 manifest 校验状态、三市场 ready 数、候选总数、人工复核数量、最终状态和建议动作。脚本生成摘要后会立即执行 manifest 结构校验，并要求三市场摘要均为 `ready`；校验失败时应视为本周自我分析失败，不把旧文件、字段不完整的文件或缺少任一市场摘要的文件当作有效结论。

自我分析还会读取 `weekly_ops_check_history.jsonl` 作为运维稳定性信号，并在 manifest 中写入 `weekly_ops_history`、`weekly_ops_history_status` 和 `weekly_ops_history_recommended_action`；当最近窗口出现重复运维问题时，该动作会进入 `automation_priority_actions`，但不会自动修改正式模型参数。
自我分析也会读取 `weekly_delivery_check_history.jsonl` 作为最终交付稳定性信号，并在 manifest 中写入 `weekly_delivery_history`、`weekly_delivery_history_status` 和 `weekly_delivery_history_recommended_action`；当最近窗口出现重复最终交付问题时，该动作会进入 `automation_priority_actions`，但不会自动修改正式模型参数。若最终交付历史中的健康原因显示人工复核积压，会写入 `review_manual_review_backlog`；若健康原因来自交付文件、自动化配置或其他非积压问题，会写入 `review_delivery_health_issues`，用于把“人工处理待办”和“流程修复问题”分开跟踪。

`automation_check_report.py` 和 `scripts/show_automation_check.ps1` 只读取 `latest_automation_check.json`，不会重新计算评分、候选池或模型审计结果；它们用于每周任务结束后的第一眼验收。

`weekly_action_items.py` 和 `scripts/show_weekly_action_items.ps1` 只读取 `outputs/automation/latest_self_analysis_manifest.json`，输出 `outputs/automation/latest_weekly_action_items.json` 和 `outputs/automation/latest_weekly_action_items.md`。该每周人工处理清单会把 `automation_priority_actions` 中的 `review_manual_queue`、`review_manual_review_backlog`、`review_delivery_health_issues`、`review_data_health`、`review_backtest_evidence`、`review_candidate_findings` 和 `continue_sample_accumulation` 翻译成可执行的人工检查事项，并保留优先级、分类、来源信号和推荐检查动作；它不抓取行情、不重新评分、不修改正式模型参数。

`codex_automation_audit.py` 和 `scripts/audit_codex_automations.ps1` 只读取本机 `C:\Users\pechen\.codex\automations` 下的 `automation.toml`，用于发现任务时间、状态、工作区或提示词漂移，不会修改自动任务。

`weekly_ops_check.py` 和 `scripts/run_weekly_ops_check.ps1` 只读取 `latest_automation_check.json`、该 JSON 中列出的关键输出路径，以及本机 `C:\Users\pechen\.codex\automations` 下的三条任务配置；它们不抓取行情、不重新评分、不修改模型参数，适合作为每周全部任务结束后的运维总闸口。该总检查默认要求 `latest_automation_check.json` 的 `as_of_date` 不超过 8 天；验收文件过期或日期晚于当前日期时会返回 `needs_attention`，不得复用旧结论。PowerShell 入口会同步写出 `outputs/automation/latest_weekly_ops_check.json`，字段包含 `ops_check_schema`、`ops_check_version`、总状态、新鲜度、关键输出缺失项和自动任务漂移问题，供后续自动任务、仪表盘或历史追踪读取；同时追加 `outputs/automation/weekly_ops_check_history.jsonl`，用于保留每次周度总检查的历史轨迹。

`weekly_ops_history_report.py` 和 `scripts/show_weekly_ops_history.ps1` 只读取 `weekly_ops_check_history.jsonl`，输出 `outputs/automation/latest_weekly_ops_history_summary.json` 和 `outputs/automation/latest_weekly_ops_history_report.md`。该摘要保留 `raw_history_count` 表示原始追加记录数，但趋势统计会按 `as_of_date` 取最后一条记录，避免同一天手动重跑被当成多周重复问题；统计最近窗口的 ready 次数、needs_attention 次数、stale 次数和重复出现的问题，用于每周自我分析判断运维状态是否稳定。它不抓取行情、不重新评分、不修改模型参数。

当 A 股或港股行情缺口属于非正 PE/PB 时，`quote_gaps.csv` 会在 `review_category` 中标记估值复核分类，例如 `loss_making_or_negative_pe`、`non_positive_book_value_or_pb` 或 `special_industry_valuation_review`。自我分析摘要会汇总这些分类，帮助区分亏损口径、净资产口径和特殊行业估值口径，仍只作为人工复核线索。

区域筛选器还会输出 `valuation_review_items.csv` 作为估值复核清单，记录被 PE/PB 质量门挡住的公司、复核分类和排除理由。自我分析摘要会从该清单抽取估值复核样例，展示股票、公司、复核分类和 PE/PB 细节，便于每周快速定位需要人工复核的代表性公司。该清单用于后续人工复核、周度对比和自我分析，不进入候选池，也不改变 `regional_fundamental_v2` 的正式评分参数。

自我分析摘要还会生成“人工复核队列”，优先汇总估值口径样例，再补充候选结论缺口和候选风险提示。该队列同时写入 `latest_manual_review_queue.csv`，字段为生成日期 `as_of_date`、优先级序号、市场、复核类型、股票、公司和复核要点；同时按周归档到 `manual_review_queue_history.csv`，同一生成日期重跑时会替换该日期旧记录，避免重复留痕。自我分析会根据历史归档识别“历史重复项”，把本周再次进入复核队列的股票和历史出现日期列入摘要，并写入 `manual_review_repeats.csv`，用于后续人工判断是否需要调整数据补齐、估值口径或复核流程。该队列用于每周人工复核的阅读顺序和历史对比，不代表自动买入、卖出或模型参数调整。

人工复核完成后，可在 `outputs/automation/manual_review_decisions.csv` 中追加处理结果。推荐字段为 `as_of_date`、`market`、`review_type`、`ticker`、`company`、`decision_status`、`decision_note`、`reviewer` 和 `decided_at`；其中 `decision_status` 建议使用 `accepted`、`rejected` 或 `needs_more_data`。统一每周结论会按本周 `latest_manual_review_queue.csv` 匹配该文件，把没有匹配到处理结果的本周队列项标为 `pending`，并汇总已处理数量、待处理数量和状态分布。同时，统一每周结论会生成 `manual_review_decisions_template.csv`，把本周队列、已有处理状态、`suggested_decision_status`、`suggested_decision_note` 和空白备注列合并成可填写模板；建议列只用于辅助人工确认，人工确认后可将模板中的有效结果追加或合并到 `manual_review_decisions.csv`。这些文件只记录人工复核结论，不触发自动买入、卖出或正式模型参数调整。

填完 `manual_review_decisions_template.csv` 后，可以运行以下命令合并到正式复核结果文件：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File F:\chatgptssd\project2\scripts\merge_manual_review_decisions.ps1
```

合并工具按 `market`、`review_type` 和 `ticker` 去重：同一复核项再次出现时会更新原记录；`pending` 或空状态不会写入正式结果；只有 `accepted`、`rejected` 和 `needs_more_data` 会进入 `manual_review_decisions.csv`。合并时会同步写出 `latest_manual_review_decision_merge.json` 和 `latest_manual_review_decision_merge.md`，记录合并/更新数量、跳过 pending 数量、跳过无效数量和状态分布。合并后再次运行 `show_weekly_conclusion.ps1`，周报会显示已处理数量、待处理数量和状态分布。

## 12个月目标价与走势跟踪

三市场周流程会在候选筛选后运行 `valuation_trend_v1`：

- PE 公允价权重 50%，PB 公允价权重 30%，美股自由现金流公允价权重 20%；缺失项目按有效权重重新分配。
- 财务质量调整系数限制在 0.85 至 1.10，12个月目标价不超过当前价的 1.60 倍。
- 高、中、低置信度分别使用 20%、25%、30% 安全边际计算建议买入价。
- 走势依据近一年日线、20/60/120 日均线、12个月动量、波动率和52周位置，分类为偏强、温和偏强、中性、偏弱或数据不足。
- `valuation_targets.csv` 保存本期目标价，`forecast_history.csv` 永久追加每周预测，`valuation_report.md` 输出中文分析。
- `latest_investment_summary.md` 会输出“候选风险说明”和“候选结论质量检查”；风险说明基于估值置信度、安全边际、走势、资产负债率、流动性、增长和数据质量自动生成，质量检查逐家公司标记低估依据、风险说明、建议买入价、目标价、跟踪状态和数据质量说明是否完整。两者只用于人工复核，不改变候选排序，也不自动修改模型参数。
- 候选行情覆盖率低于 80% 时估值步骤失败，不更新本次正式摘要，也不得把旧报告当作本次结果。

## 预测跟踪与模型审计

三市场周流程在目标价生成后继续执行预测跟踪和模型审计：

- 使用股票复权收盘价，并分别对照美股标普500、A股沪深300、港股恒生综合指数。
- 每周更新 `tracking_snapshot.csv`；在预测后4、12、26、52周生成正式评价，写入不可覆盖、按唯一键去重的 `forecast_evaluations.csv`。
- 同时评价绝对收益、相对基准超额收益、目标价误差、最大有利/不利波动和方向命中率；方向变化超过正负5%才判定为上涨或下跌。
- `performance_report.md` 汇总本周跟踪结果，`model_audit.md` 记录每周自我分析，`shadow_model_proposals.csv` 只保存影子分析候选。
- 成熟评价少于30条时只显示“样本积累中”；验证集少于15条或市场覆盖不足2个时，不生成影子参数候选。
- 影子候选仅用于后续样本外验证。没有明确的验证集改善和风险不恶化证据时，不得标记为可升级，更不得自动修改 `valuation_trend_v1` 正式模型。

以上结果仅供研究筛选，不构成投资建议或自动交易指令。

## 美股严格时点回测

严格时点回测用于验证 `valuation_trend_v1` 在历史样本中的表现，默认只做 8 个分散周次试跑，不接入正式每周筛选任务，也不得自动升级正式模型。

试跑命令：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File F:\chatgptssd\project2\scripts\run_us_point_in_time_backtest.ps1 -PilotWeeks 8 -SecUserAgent "Your Name your-email@example.com"
```

只检查执行计划，不联网、不写入文件：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File F:\chatgptssd\project2\scripts\run_us_point_in_time_backtest.ps1 -PilotWeeks 8 -SecUserAgent "Your Name your-email@example.com" -DryRun
```

完整 156 周运行必须在试跑通过后显式指定：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File F:\chatgptssd\project2\scripts\run_us_point_in_time_backtest.ps1 -FullRun -SecUserAgent "Your Name your-email@example.com"
```

主要输出目录为 `outputs/backtests/us_3y_weekly`，关键文件包括：

```text
outputs/backtests/us_3y_weekly/historical_membership.csv
outputs/backtests/us_3y_weekly/replay_manifest.csv
outputs/backtests/us_3y_weekly/checkpoint.json
outputs/backtests/us_3y_weekly/backtest_forecasts.csv
outputs/backtests/us_3y_weekly/backtest_evaluations.csv
outputs/backtests/us_3y_weekly/model_comparison.csv
outputs/backtests/us_3y_weekly/backtest_report.md
outputs/backtests/us_3y_weekly/data_leakage_audit.md
outputs/automation/latest_backtest_summary.md
```

当 `historical_membership.csv` 已经存在，但 `price_history.csv` 或 `benchmark_history.csv` 缺失或为空时，脚本会自动调用 `backtest_price_inputs.py` 准备 5 年股票历史行情和标普 500 基准历史行情；如果覆盖率低于 80%，不会覆盖已有正式输出。
当 `historical_membership.csv` 缺失或为空时，脚本会先调用 `backtest_membership_inputs.py` 基于本地 `us_universe_symbols.csv` 生成带 CIK 的周度成员名单；该名单证据等级为 `secondary`，只用于机械试跑，不得作为模型升级依据。
历史成分证据包可放在 `data/config/us_sp500_membership_evidence.csv`。字段为 `effective_date, added_ticker, removed_ticker, membership_evidence, membership_source_url, notes`。只有官方 S&P Global HTTPS 来源可以保留 `verified`；非官方来源即使写入 `verified` 也会自动降级为 `secondary`。证据包缺失时，脚本仍按当前股票池生成 `secondary` 名单，仅用于机械试跑。
`backtest_report.md` 会汇总本次回放窗口的成员证据覆盖情况，包括 `verified` 比例、`secondary` 数量、`insufficient` 数量、弱证据行数和存在弱证据的回放周数；该信息用于判断历史证据是否足以支持后续模型升级。
`outputs/automation/latest_backtest_summary.md` 是严格时点回测的短摘要入口，记录完成周数、失败周数、成员证据覆盖、弱证据行数、`Evidence status`、`Weak evidence weeks`、`Evidence next action`、回测报告、泄漏审计、模型对比和日志路径。自我分析会读取这些字段写入 manifest，用于判断是否需要优先补充 verified 历史成分证据；存在弱证据时仍不得自动升级正式模型。
当 `data/cache/sec_companyfacts` 缺失或为空时，脚本会调用 `backtest_sec_cache.py` 按成员名单中的 CIK 准备 SEC Company Facts 缓存；如果覆盖率低于 80%，回测会停止，避免用严重缺失的财务事实继续生成结论。
需要先做小样本真实联调时，可临时传入 `-MaxCompanies 3` 等参数限制准备阶段的公司数量；默认值为 0，表示正式运行不限制公司数量。

质量门要求：

- 历史成分证据必须区分 `verified`、`secondary`、`insufficient`，二手或证据不足周次不得作为升级依据。
- 单周回放只能使用 `generated_date` 当日已经可用的成分、SEC 申报和行情数据；`data_leakage_audit.md` 出现严重泄漏时，该周不可用于模型升级判断。
- `backtest_forecasts.csv` 以市场、股票、生成日、模型版本为唯一键；`backtest_evaluations.csv` 还要加检查点周数和评价版本去重。
- `model_comparison.csv` 只比较正式模型和影子参数方案；结论即使出现 `review_candidate`，也只能进入人工复核，不得自动升级正式模型。
